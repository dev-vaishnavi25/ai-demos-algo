version: "3.9"

services:
  redis:
    image: redis:7-alpine
    container_name: ai-demos-redis

    ports:
      - "6379:6379"

    restart: unless-stopped

    command: redis-server --appendonly yes

    volumes:
      - redis-data:/data

volumes:
  redis-data: